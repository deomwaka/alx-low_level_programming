#include "main.h"
/**
 * _abs - prints the absolute value of an interger
 * @n: n is an integer
 * 
 * Return: int
 */

int _abs(int n)
{
	if (n > 0)
	{
		return (n);
	}
	else if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
}
